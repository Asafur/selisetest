# DATADUMP Extraction

Last updated: 2026-05-12

This is the safe canonical extraction from `C:\Users\akkha\Desktop\DATADUMP.txt` and the latest assignment prompt. The raw dump is reference material only. Do not execute it line by line, copy it into this repo, or commit it.

## 1. Canonical Project Goal

Build **VibeBuilder - Drag and Drop**, a professional multi-user SaaS website builder powered by SELISE Blocks. Users register/login with SELISE IAM, create website projects, create multiple pages, build pages visually with drag-and-drop Vibe Components, upload media through SELISE Storage/Media, autosave layouts to SELISE Data Gateway, publish pages, and view public live sites through a renderer that is separate from the editor.

## 2. Final Functional Requirements

- Authenticated users can create, list, edit, delete, and publish their own website projects.
- The first signed-in `@gmail.com` account should be the only app admin, enforced through SELISE IAM role assignment rather than a browser-only flag.
- Each website contains multiple pages with page title, slug, homepage flag, draft layout, and published layout.
- The builder has a top save/preview/publish bar, left component library, center drag-and-drop canvas, and right settings panel.
- Users can drag components from the library, reorder components, select components, edit properties, remove components, autosave, manually save, preview, and publish.
- Required Vibe Components: Hero, Text, Image, Gallery, Contact Form, CTA, FAQ, Feature Cards, Testimonials, Navbar/Header, Footer, Pricing, Team, Map/Location, Video.
- Layouts serialize as versioned JSON with block `id`, `type`, `props`, `style`, and optional `children`.
- Public renderer loads only published content and supports navigation between pages.
- Contact form submissions must go through SELISE data services, not console logging or fake APIs.

## 3. Final Technical Constraints

- Use the existing Construct React/Vite/TypeScript project if present.
- Use SELISE IAM for registration, login, current user, and access-control foundation.
- Use SELISE Data Gateway/Content/Data services for Website, Page, Theme, Asset, Role, and ContactSubmission persistence.
- Use SELISE Storage/Media for all uploaded images and media.
- Do not use Firebase, Supabase, Appwrite, local SQL, direct Mongo access, Express/custom backend, fake APIs, mock persistence, static JSON files as storage, or localStorage as the real database.
- Keep SELISE integration code separate from UI components.
- Keep real secrets only in ignored local env files.
- If SELISE schemas/services are missing, document exact setup blockers and show real error/blocked states instead of faking success.

## 4. SELISE Services Required

- IAM/Authentication: email/password or SSO login, user identity, account lookup, roles/permissions if configured.
- Data Gateway: GraphQL-centered schemas and CRUD operations for VibeBuilder data.
- Storage/Media/DMS: file upload, serving, listing, and deletion if supported.
- Deployment/Hosting: Construct-based deployment and public app hosting.
- Localization: optional for UI text, but not required for core builder data.
- Observability: useful for deployment and runtime diagnostics.

## 5. Known Project Config Values Found

- Project slug: `pnuasg`
- App/deployment domain: `https://pnuasg-dzdlq.seliseblocks.com`
- Blocks API URL: `https://api.seliseblocks.com` / `api.seliseblocks.com`
- GitHub repo clue: `https://github.com/Asafur/selisetest`
- Reference-only repo clue: `https://github.com/kazi-lakit/stg-test`
- Active local project found: `C:\Users\akkha\selisetest-local-run`
- Local env files contain the expected Vite keys, but real values are intentionally not copied here.

## 6. Credentials/Sensitive Values Found

- SELISE X-Blocks-Key-like value: `P8d****63f`
- At least one email-like value in the dump was detected and masked.
- Local `.env*` files contain project configuration and are ignored by Git.

Never print, commit, or document full keys, tokens, passwords, client secrets, private env values, or raw dump text.

## 7. Existing Project/Repo Clues

- Continue in `C:\Users\akkha\selisetest-local-run`; do not create a duplicate project.
- The project is a SELISE Construct React app generated by Blocks CLI.
- Existing app uses React, Vite, TypeScript, Tailwind, shadcn-style UI components, Zustand auth store, TanStack Query, and a SELISE HTTP/GraphQL client layer.
- Existing `src/features/site-builder` folder is the active VibeBuilder feature module and should be updated in place.
- `kazi-lakit/stg-test` can be inspected only as a reference if needed; it is not the project source.

## 8. Routes Required

Protected admin/editor routes:

- `/admin`
- `/admin/sites`
- `/admin/sites/new`
- `/admin/sites/:siteId`
- `/admin/sites/:siteId/pages`
- `/admin/sites/:siteId/pages/new`
- `/admin/sites/:siteId/pages/:pageId/builder`
- `/admin/sites/:siteId/media`
- `/admin/sites/:siteId/theme`
- `/admin/sites/:siteId/settings`
- `/admin/sites/:siteId/users`

Public/live routes:

- `/vibe/:siteSlug`
- `/vibe/:siteSlug/:pageSlug`

Preview route:

- `/preview/:siteId/:pageId`

Use `/vibe/*` public routes to avoid conflicts with Construct auth/admin routes.

## 9. Data Schemas Required

Current project schemas discovered by live introspection on 2026-05-05:

- `VibeProject`: used for Website/Site records.
- `VibePage`: used for page records and layout envelope storage.
- `VibeFormSubmission`: used for contact form submissions.

Current MVP fallback:

- Theme settings and media asset index are stored in encoded `VibeProject.Tags` metadata because separate `VibeTheme` and `VibeAsset` schemas are not present.
- `VibePage.Layout` is a string field containing draft/published layout JSON.

Canonical long-term schema architecture:

Workspace or Tenant:

- `id`, `ownerUserId`, `name`, `slug`, `createdAt`, `updatedAt`

Site:

- `id`, `workspaceId`, `ownerUserId`, `name`, `slug`, `description`, `logoAssetId`, `logoUrl`, `themeId`, `status`, `homepagePageId`, `createdAt`, `updatedAt`

Page:

- `id`, `workspaceId`, `siteId`, `ownerUserId`, `title`, `slug`, `status`, `layoutJson`, `draftLayoutJson`, `publishedLayoutJson`, `themeOverrideJson`, `sortOrder`, `isHomePage`, `createdAt`, `updatedAt`, `publishedAt`

Theme:

- `id`, `workspaceId`, `siteId`, `name`, `templateType`, `colorsJson`, `typographyJson`, `spacingJson`, `buttonStyleJson`, `sectionStyleJson`, `headerStyleJson`, `footerStyleJson`, `createdAt`, `updatedAt`

Asset:

- `id`, `workspaceId`, `siteId`, `ownerUserId`, `fileName`, `fileUrl`, `fileType`, `fileSize`, `mediaBlockId`, `uploadedBy`, `createdAt`, `updatedAt`

UserRole:

- `id`, `workspaceId`, `siteId`, `userId`, `role`, `createdAt`, `updatedAt`

ContactSubmission:

- `id`, `workspaceId`, `siteId`, `pageId`, `blockId`, `name`, `email`, `phone`, `message`, `createdAt`

Optional PublishRecord:

- `id`, `workspaceId`, `siteId`, `pageId`, `publishedBy`, `publishedLayoutJson`, `publishedThemeJson`, `publishedAt`

## 10. Things to Ignore From DATADUMP

- Duplicate old prompt versions and repeated class notes.
- Earlier weaker MVP ideas that conflict with the final assignment.
- Fake/mock data suggestions.
- Random repo install suggestions.
- `kazi-lakit/stg-test` as a source project.
- Blog/post CMS, plugin marketplace, custom domain, SEO suite, billing, payment, and unrelated platform research unless explicitly requested later.
- Table, code-embed, and blog/article-style blocks are not part of the final required Vibe Component set; keep them out of the draggable MVP library even if legacy saved-layout renderers remain tolerant.
- Raw credentials and setup commands that expose full secrets.
- Route ideas that conflict with safe `/admin/*` and `/vibe/*` routing.

## 11. Open Blockers / Manual SELISE Setup Needed

- Confirm or create Data Gateway schemas listed above.
- Confirm the exact GraphQL gateway endpoint from Data Gateway Preview; the existing generic endpoint previously returned 404.
- Configure RLS/CLS or equivalent access rules so users only access records matching their owner/workspace.
- Confirm JSON/object field support; otherwise approve stringified JSON fields for layouts/themes.
- Configure IAM signup/login and user account policy.
- Configure Owner/Editor/Viewer roles and permissions if role-based collaboration is required.
- Run the first-Gmail-only admin bootstrap with a current SELISE admin bearer access token after at least one Gmail account has signed in.
- Configure SELISE Storage/Media and document upload/list/delete URL flow.
- Configure contact-submission schema and write access for public form submissions.
- Configure Construct-based deployment in Blocks Cloud for the connected repository.
